{
  "name": "@strawberrybear/keystroke-sequencer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --api false",
    "lint": "eslint .",
    "clean": "rimraf dist .turbo"
  },
  "devDependencies": {
    "@strawberrybear/tsconfig": "workspace:*",
    "typescript": "~5.7.2",
    "vitest": "^3.2.4"
  }
}
