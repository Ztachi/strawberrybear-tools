---
'@strawberrybear/infinity-nikki-player': patch
---

独立音轨窗口标题栏增加播放队列入口，与全局播放器共用 antdv 队列按钮和列表内容，保留主窗口原有 Drawer，浮窗使用自己的固定工作区挂载点。队列摘要和当前曲同步，选歌由主窗口调用既有预览动作，并拒绝过期队列请求；去掉抽屉打开时影响祖先容器的自动滚动。打开浮窗自动启用详情跟曲，之后仍可手动关闭；不改变游戏按键模拟。
