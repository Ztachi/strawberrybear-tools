---
'@strawberrybear/piano-roll': patch
'@strawberrybear/infinity-nikki-player': patch
---

将自动播放视口统一为曲首、固定中线和曲尾三段投影，手动浏览保留真实视口与不可见播放头。各图层按同一帧坐标提交，琴键绘制与横向播放解耦，滚动只写入实际操作轴。横向手势确认不再冻结自动播放，避免双指纵向尾帧在高缩放下引起指针跳动。预览进度按显示帧读取现有音频时钟，并保留后台曲终检测与会话清理；不修改游戏按键模拟调度。

标尺取消逐帧整数取整，刻度密度和标签避让改为稳定的时间轴规则，修复低缩放滚动跳动、标签闪烁及 tempo 进出视口时的刻度切换。
