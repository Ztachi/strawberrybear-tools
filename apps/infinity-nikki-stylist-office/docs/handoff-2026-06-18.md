# infinity-nikki-stylist-office 阶段交接记录

记录时间：2026-06-18 17:35（Asia/Shanghai）

## 当前完成的小阶段

本阶段完成的是“项目骨架可运行/可构建”。

已落地内容：

- 新建 `apps/infinity-nikki-stylist-office`，包名为 `@strawberrybear/infinity-nikki-stylist-office`。
- 新建 `packages/nikki-theme`，抽出无限暖暖共享主题 token，保持无 Vue/Vuetify/antdv 依赖。
- 接入技术栈：Vue 3、Vite 8、TypeScript、Vuetify、Tailwind CSS 4、Pinia、`pinia-plugin-persistedstate`、Vue Router、vue-i18n、TanStack Query、Dexie、Zod、Vitest。
- 接入响应式基础页面：
  - 首页
  - 登记资料
  - 签发前校样
  - 签发仪式
  - 证书正本
  - 个人中心
  - 自定义头像管理
  - 自定义背景管理
- 顶部栏已按要求搭好：
  - 左侧 logo + 标题
  - 右侧语言下拉
  - 右侧个人中心入口
- UI 语言和证书语言已按规则分离：
  - 顶部语言只控制 UI。
  - `uiStore.lastCertificateLocale` 保存新草稿默认证书语言。
  - 登记页单独展示证书语言选择。
- IndexedDB 数据边界已搭好：
  - 草稿、正式证书、自定义素材、资料库缓存、证书 PNG 正本、设置表。
  - Blob 和大型 PNG 不进入 Pinia 持久化。
- 协会资料库 seed 与 schema 已搭好：
  - 8 个搭配师称号已写入。
  - 模板、地区、评语、头像、背景、图片资源保留接口形态。
- 复制产品需求文档到 `apps/infinity-nikki-stylist-office/docs/product/mvp-requirements.md`。
- 新增资料库契约文档 `docs/association-catalog-schema.md`。
- 新增发布 workflow：`.github/workflows/release-infinity-nikki-stylist-office.yml`。

## 验证结果

已通过：

```bash
pnpm --filter @strawberrybear/nikki-theme type-check
pnpm --filter @strawberrybear/nikki-theme lint
pnpm --filter @strawberrybear/nikki-theme build
pnpm --filter @strawberrybear/infinity-nikki-stylist-office test
pnpm --filter @strawberrybear/infinity-nikki-stylist-office type-check
pnpm --filter @strawberrybear/infinity-nikki-stylist-office build
git diff --check
```

`pnpm --filter @strawberrybear/infinity-nikki-stylist-office lint` 当前退出码为 0，但还有 Vue 模板格式 warning，主要是 `vue/max-attributes-per-line` 和 `vue/singleline-html-element-content-newline`。这些 warning 不影响构建，后续可以用手工调整模板换行清理。

## 重要文件

- 新 app 入口：`apps/infinity-nikki-stylist-office/src/app/main.ts`
- 根组件：`apps/infinity-nikki-stylist-office/src/app/App.vue`
- 路由：`apps/infinity-nikki-stylist-office/src/app/router.ts`
- Pinia：`apps/infinity-nikki-stylist-office/src/app/pinia.ts`
- UI store：`apps/infinity-nikki-stylist-office/src/stores/ui.ts`
- IndexedDB：`apps/infinity-nikki-stylist-office/src/db/database.ts`
- 资料库 seed：`apps/infinity-nikki-stylist-office/src/data/associationCatalog.seed.ts`
- 资料库 schema：`apps/infinity-nikki-stylist-office/src/domain/catalog/schema.ts`
- 顶部栏：`apps/infinity-nikki-stylist-office/src/components/AppHeader.vue`
- 语言切换：`apps/infinity-nikki-stylist-office/src/components/LanguageSwitcher.vue`
- 共享主题包：`packages/nikki-theme/src/index.ts`

## 下一阶段建议顺序

1. 清理新 app 的 Vue lint warning，先处理组件模板换行，不改业务结构。
2. 启动本地 dev server，做桌面 `1440x900` 和移动 `390x844` 的浏览器截图检查。
3. 实现草稿创建服务：
   - 首页点击登记资料时创建唯一草稿。
   - 证书语言默认取当前 UI 语言。
   - 姓名、称号、证书语言、地区等写入 Dexie。
4. 接入资料库远程更新：
   - 先从本地 seed 读取。
   - 再接 `VITE_ASSOCIATION_DATA_BASE_URL` 拉取 manifest。
   - 使用 Zod 校验后写入 Dexie。
5. 进入模板阶段：
   - 使用分层素材：背景层、头像层、透明模板覆盖层、文字层。
   - 模板坐标基准统一为 `3840 × 2160`。
   - 先实现预览，再实现 16:9 PNG 和 A4 PNG 导出。

## 注意事项

- `pinia-plugin-persistedstate` 只用于轻量 UI 偏好，不要保存 Blob、完整证书 PNG、模板图片或大型草稿快照。
- 内容型多语言当前先中文占位；UI 文案已经按四语言文件组织。
- 当前构建产物 `apps/infinity-nikki-stylist-office/dist` 已被 app 内 `.gitignore` 忽略，不需要提交。
- `pnpm install` 已更新 `pnpm-lock.yaml`。
